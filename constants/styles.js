import { StyleSheet } from "react-native";

// styles that are common app-wide

export default StyleSheet.create({
    textInput: {
        width: "100%",
        borderWidth: 1,
        borderColor: "#0002",
        fontSize: 16,
        paddingVertical: 6,
        paddingHorizontal: 6,
        fontFamily: "poppins-",
    },
});
